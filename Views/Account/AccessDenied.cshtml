@{
    ViewData["Title"] = "Từ chối truy cập";
    // Giả sử bạn có một layout chung, nếu không có thì có thể bỏ dòng này
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .access-denied-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 50px 20px;
        min-height: 70vh;
    }

    .access-denied-icon {
        font-size: 5rem;
        color: #e74a3b; /* Red */
    }

    .access-denied-title {
        font-size: 2.5rem;
        font-weight: 700;
        margin-top: 1.5rem;
        color: #3a3b45;
    }

    .access-denied-text {
        font-size: 1.2rem;
        color: #858796;
        max-width: 500px;
        margin-top: 1rem;
        margin-bottom: 2rem;
    }
</style>

<div class="access-denied-container">
    <div class="access-denied-icon">
        <i class="fas fa-user-lock"></i>
    </div>
    <h1 class="access-denied-title">Truy cập bị từ chối</h1>
    <p class="access-denied-text">
        Rất tiếc, bạn không có quyền truy cập vào trang này. Vui lòng liên hệ với quản trị viên nếu bạn cho rằng đây là một sự nhầm lẫn.
    </p>
    <a asp-action="Index" asp-controller="Home" class="btn btn-primary">
        <i class="fas fa-home me-2"></i>
        Quay về Trang chủ
    </a>
</div>

